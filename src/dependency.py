

import pygame, random, pickle, os
import src.game, src.reader, src.blocks, src.checkbox, src.tempreader, src.reader 

# Helper function Transforms list of lists into one list with 2 for loops
def forfor(a):
    return [item for sublist in a for item in sublist]
